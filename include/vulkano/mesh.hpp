#pragma once

#include <cstdint>
#include <vector>

namespace vulkano {

struct Vertex final {
    float position[3];
    float normal[3];
    float tangent[3];
    float bitangent[3];
    float texcoord[2];
};

struct Mesh final {
    std::vector<Vertex> vertices;
    std::vector<std::uint32_t> indices;
};

} // namespace vulkano

